---
const { title, tagline } = Astro.props;
---

<div
  class="relative h-[360px] mb-4 text-left brutal bg-cover bg-center bg-[url('/movies/masthead-movie.gif')]"
>
  <div class="absolute bottom-8 px-8">
    <h1 class="text-3xl text-gold">{title}</h1>
    <p class="text-sand">{tagline}</p>
  </div>
</div>
